# Workstreams

Workstreams define ownership boundaries for parallel implementation.

Each workstream documents what it owns, what it does not own, dependencies, and acceptance criteria.

- application setup
