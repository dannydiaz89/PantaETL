# APP-007 — Application Skeleton CI Validation

**Status:** COMPLETE
**Owner:** Codex
**Workstream:** Application Setup  
**Depends on:** APP-005, APP-006, FOUNDATION-004

## Scope

Extend CI to validate that the complete application skeleton remains buildable.

- Build/typecheck all TypeScript apps/packages.
- Validate Python worker.
- Validate Compose configuration.

## Out of scope

- Unrelated workstreams.
- Product feature implementation beyond this task.
- Architecture changes not required by this task.

## Acceptance criteria

- [x] CI catches broken service/package boundaries.
- [x] No deployment pipeline is required.

## Validation

Run all checks relevant to the packages/services introduced or changed by this task.

- `pnpm check`
- `uv sync --frozen && uv run ruff check . && uv run ruff format --check . && uv run mypy . && uv run pytest`
- `docker compose config --quiet`
- `actionlint .github/workflows/quality.yml`

## Notes / blockers

Added a dedicated Compose configuration job to the quality workflow. The existing
TypeScript and Python jobs continue to validate every application/package boundary
and the worker without introducing a deployment pipeline.
