# APP-006 — Docker Compose Development Topology

**Status:** COMPLETE
**Owner:** Codex
**Workstream:** Application Setup  
**Depends on:** APP-002, APP-003, APP-004

## Scope

Create baseline Compose topology for the self-hosted application.

- PostgreSQL service.
- Web service.
- Scheduler service.
- Worker service.
- Garbage collector service.
- Local persistent storage mounts.

## Out of scope

- Unrelated workstreams.
- Product feature implementation beyond this task.
- Architecture changes not required by this task.

## Acceptance criteria

- [x] Compose configuration validates.
- [x] Service topology reflects architecture.
- [x] No Redis/RabbitMQ/etc. introduced.

## Validation

Run all checks relevant to the packages/services introduced or changed by this task.

## Notes / blockers

Added the development Compose topology for PostgreSQL, web, scheduler, worker, and
garbage collector services, with persistent PostgreSQL/application-storage volumes,
health-gated database dependencies, and pinned Node/uv service build environments.
Validated with `docker compose config`, image builds, a full stack smoke test, and
the repository quality checks.
