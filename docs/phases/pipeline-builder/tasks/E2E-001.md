# E2E-001 — Real Create Configure Save Reload Browser Workflow

**Status:** COMPLETE
**Owner:** Claude
**Depends on:** BUILDER-007, VALID-003

## Scope

Implement only this task. Inspect current code before adding abstractions; preserve canonical contracts, registries, CRUD API, pipeline-domain rules, security, localization, accessibility, and design-system boundaries.

## Acceptance criteria

- [x] Create with real Source.
- [x] Configure at least one Transform.
- [x] Configure compatible Export.
- [x] Save through real API.
- [x] Reload verifies persistence.
- [x] Draft/readiness behavior verified.
- [x] No pipeline mock required.

## Required checks

- Relevant unit/integration tests.
- TypeScript typecheck/lint for TS changes.
- Ruff, mypy, pytest for Python changes.
- Contract/catalog generation consistency when applicable.
- Accessibility checks for UI changes.
- Localize all user-facing strings.
- Add useful descriptions for exported/public and non-trivial functions.
- Do not reference task IDs/planning docs in implementation comments or commit messages.

## Notes / blockers

While building the real, unmocked browser session helper, real email/password sign-in was
found to be completely broken: the local authentication configuration's Drizzle schema map used
a model key that did not match its configured model name, and its session/account identifiers
did not match the database's UUID columns. Both were fixed alongside this task since no real
session could be established otherwise; every other check (unit tests, lint, typecheck, other
browser tests, Python checks) was re-verified afterward.
