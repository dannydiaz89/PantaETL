# Codex Prompt

Use this to start the phase:

Read `AGENTS.md`, the existing architecture documentation, and the Pipeline CRUD & UI Wiring documentation pack. Treat `PHASE_ROADMAP.md` as the tracker for this phase. Begin with the first READY task. Before implementation, inspect the current repository code referenced by that task and preserve existing architecture rather than creating parallel models. Mark the task and phase roadmap IN PROGRESS, complete only that task and all acceptance criteria, run the required checks, then mark it COMPLETE. Do not begin another task unless explicitly assigned.

For parallel agents after CRUD-001:

Read `AGENTS.md` and the Pipeline CRUD & UI Wiring docs. Claim only the assigned READY task, verify dependencies, update its status/owner, and stay within scope. Preserve canonical contracts and shared domain rules. Do not reference task IDs or documentation in implementation comments or commit messages.
