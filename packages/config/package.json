{
  "name": "@pantaetl/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "test": "vitest run tests"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
