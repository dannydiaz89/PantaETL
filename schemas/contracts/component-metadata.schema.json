{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Canonical contract definition. Generated language artifacts must not be edited manually.",
  "title": "ComponentMetadata",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "source",
        "transform",
        "export"
      ]
    },
    "type": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"
    },
    "version": {
      "type": "string",
      "pattern": "^v\\d+$"
    },
    "displayNameKey": {
      "type": "string",
      "minLength": 1
    },
    "descriptionKey": {
      "type": "string",
      "minLength": 1
    },
    "configFields": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^[a-z][a-zA-Z0-9_]*$"
          },
          "type": {
            "$comment": "A file field holds the same relative path text as a text field; the control additionally offers to supply the file.",
            "type": "string",
            "enum": [
              "text",
              "textarea",
              "number",
              "boolean",
              "select",
              "json",
              "file"
            ]
          },
          "labelKey": {
            "type": "string",
            "minLength": 1
          },
          "descriptionKey": {
            "type": "string",
            "minLength": 1
          },
          "required": {
            "type": "boolean"
          },
          "secret": {
            "type": "boolean"
          },
          "width": {
            "$comment": "How much horizontal room the value needs, so a renderer need not guess from the control type.",
            "type": "string",
            "enum": [
              "short",
              "medium",
              "full"
            ]
          },
          "defaultValue": {
            "$comment": "The value the executing component applies when the field is left unset. Renderers show it so the form never contradicts execution.",
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number"
              },
              {
                "type": "boolean"
              }
            ]
          },
          "options": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "value": {
                  "type": "string",
                  "minLength": 1
                },
                "labelKey": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "required": [
                "value",
                "labelKey"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "key",
          "type",
          "labelKey",
          "required",
          "secret"
        ],
        "additionalProperties": false
      }
    },
    "inputFamilies": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "any",
          "document",
          "tabular",
          "file"
        ]
      }
    },
    "outputFamilies": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "any",
          "document",
          "tabular",
          "file"
        ]
      }
    }
  },
  "required": [
    "kind",
    "type",
    "version",
    "displayNameKey",
    "descriptionKey",
    "configFields",
    "inputFamilies",
    "outputFamilies"
  ],
  "additionalProperties": false,
  "$id": "https://pantaetl.dev/schemas/component-metadata.schema.json"
}
